namespace CyclotronFunction;

class Program
{
    static void Main(string[] args)
    {
        Cyclotron result = new();

        result.GetParticleFirstLetter();

        result.GetMatrixLength();

        result.PrintMatrix();
    }
}
